{
  "name": "__PROJECT_SLUG__",
  "version": "0.1.0",
  "private": true,
  "description": "__PROJECT_NAME__ — SPA template project",
  "scripts": {
    "build": "node --check src/app/app.js && node scripts/build.mjs",
    "check": "node --check src/app/app.js && node scripts/build.mjs --check",
    "start": "node server/server.js",
    "test": "echo \"Add tests here\"",
    "ship:check": "node scripts/check-ship.mjs"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
