{
  "name": "projects-web-demo-api",
  "version": "0.1.0",
  "private": true,
  "description": "Small local API for the Projects web demo.",
  "scripts": {
    "start": "node server.js",
    "preview": "node static.js",
    "check": "node --check server.js && node --check static.js",
    "protect:check": "node ../scripts/check-protected-frontend.mjs",
    "assets:check": "node ../scripts/check-public-assets.mjs",
    "static:check": "node ../scripts/check-static-preview.mjs",
    "routes:check": "node ../scripts/check-public-routes.mjs",
    "sync:check": "node ../scripts/check-sync-surface.mjs",
    "recovery:check": "node ../scripts/check-state-recovery.mjs",
    "docker:check": "node ../scripts/check-docker-boundary.mjs",
    "boundary:check": "node ../scripts/check-public-assets.mjs && node ../scripts/check-public-boundary.mjs",
    "ship:check": "node ../scripts/check-ship.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "acorn": "^8.17.0",
    "terser": "^5.48.0"
  }
}
